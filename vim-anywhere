#!/usr/bin/env bash

terminal=$1
vim=$2

function setclip {
  xclip -selection c
}

function getclip {
  xclip -selection clipboard -o
}

file=$(mktemp)
$terminal -e $vim "$file"

cat $file | setclip
rm $file

xdotool key ctrl+shift+v
